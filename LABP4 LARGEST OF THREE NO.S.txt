PROGRAM LARGEST OF THREE NUMBERS
#include<stdio.h>
#include<stdio.h>
#include<math.h>
void main()
{
    int a,b,c;
    printf("enter three number:");
    scanf("%d %d %d", &a,  &b, &c);
    if(a>b&&a>c)
    {
    printf("\n %d is large",a);
    }
    else
    if(b>c&&b>c)
    {
    printf("\n %d is large",b);
    }
    else
    printf("\n %d is large",c);
    getch();
}
